import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Pedidos-mobile',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
